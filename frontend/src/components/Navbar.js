import React from 'react';

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1>HRMS Lite Admin</h1>
    </nav>
  );
};

export default Navbar; // This is the line that fixes your error